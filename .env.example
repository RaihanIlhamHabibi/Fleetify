# Database
DB_HOST=mysql
DB_PORT=3306
DB_USER=fleetify
DB_PASSWORD=fleetify123
DB_NAME=fleetify

# Application
APP_PORT=8080
UPLOAD_DIR=./uploads
WEBHOOK_URL=

# Seeder (set true on first run to seed if DB is empty)
RUN_SEEDER=true
